.image-card {
    position: relative;
    width: 350px;
}

.image-card img {
    width: 100%;
    display: block;
    border-radius: 10px;
}

/* Places the View button over the picture */
#viewButton {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);

    padding: 10px 25px;
    border: none;
    border-radius: 5px;
    background-color: black;
    color: white;
    cursor: pointer;
}

#viewButton:hover {
    background-color: #333;
}

/* The preview window */
#imageModal {
    width: min(600px, 85%);
    padding: 25px;
    border: none;
    border-radius: 12px;
    text-align: center;
}

#imageModal img {
    width: 100%;
    max-height: 350px;
    object-fit: contain;
    border-radius: 8px;
}

#imageModal::backdrop {
    background-color: rgba(0, 0, 0, 0.75);
}

.close-button {
    position: absolute;
    top: 5px;
    right: 12px;

    border: none;
    background: transparent;
    font-size: 30px;
    cursor: pointer;
}